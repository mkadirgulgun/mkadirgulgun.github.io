@if (ViewData["Hata"] == null)
{
    <div class="alert alert-success" role="alert">
        Satın alma başarılı!
    </div>
}
else
{
    <div class="alert alert-danger">
        @ViewData["Hata"]
    </div>
}